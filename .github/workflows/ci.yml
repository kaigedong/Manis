name: CI

on:
  push:
    branches: [main]
  pull_request:
    types: [opened, synchronize, reopened, closed]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.event_name == 'pull_request' && format('pr-{0}', github.event.pull_request.number) || github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always

jobs:
  quality:
    if: github.event_name != 'pull_request' || github.event.action != 'closed'
    name: Format, lint, and test (macOS)
    runs-on: macos-14
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Install Rust toolchain
        run: rustup show active-toolchain
      - name: Check formatting
        run: cargo fmt --all -- --check
      - name: Check all targets
        run: cargo check --workspace --all-targets --locked
      - name: Check snapshot fixture harness
        run: cargo check -p manis-ui --example snapshot --features snapshot-fixtures --locked
      - name: Run tests
        # Keep successful runs readable while preserving libtest's failure details.
        run: cargo test --workspace --all-targets --locked --quiet
      - name: Run Clippy
        run: cargo clippy --workspace --all-targets --locked -- -D warnings

  platform-check:
    if: github.event_name != 'pull_request' || github.event.action != 'closed'
    name: Check ${{ matrix.name }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Linux
            os: ubuntu-latest
          - name: Windows
            os: windows-latest
    runs-on: ${{ matrix.os }}
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Install Linux system dependencies
        if: runner.os == 'Linux'
        run: >-
          sudo apt-get update && sudo apt-get install --yes
          libasound2-dev libfontconfig1-dev libgtk-3-dev libwayland-dev
          libx11-xcb-dev libxcb-render0-dev libxcb-shape0-dev
          libxcb-xfixes0-dev libxkbcommon-dev
      - name: Install Rust toolchain
        run: rustup show active-toolchain
      - name: Check workspace
        run: cargo check --workspace --all-targets --locked
      - name: Test Linux profile rendering
        if: runner.os == 'Linux'
        run: cargo test -p manis-profile --locked --quiet
