# Manis · Signal Patch Bay 设计系统

> 说明：`Manis` 是已确认的产品名，取名自穿山甲属；本文记录已落地的视觉方向与 GPUI 交互约束。

## 设计意图

Signal Patch Bay 把“路由决策”做成一条可见信号链，而不是把产品做成 VPN 面板或 YAML 编辑器。

- 先解释“为什么这样走”，再暴露具体内核术语。
- 先让用户改策略组和节点，再考虑配置文件。
- 把路由事实和路由预测分开，不把界面推演包装成已观察结果。
- 把窄窗口当成真实工作模式，而不是把三栏硬缩小。

## 信息层级

核心对象只有三层：`规则 -> 策略组 -> 最终节点`。

| 层级 | 用户看到什么 | 主要动作 | 说明 |
|---|---|---|---|
| 规则 | 命中顺序、规则类型、payload | 解释、筛选 | 只负责“为什么” |
| 策略组 | 手动选择、自动选择、故障转移、负载均衡、直连 | 切换节点、测速 | 这是用户最常操作的对象 |
| 节点 | 来源、延迟、标签、当前选中态 | 选择出口 | 只呈现可选出口，不复制领域状态 |

视觉上，导航、列表、详情、解释器是四个清楚分层的区域。铜色路线只用于“被解释的路径”，不作为普通强调色。

## 视觉基线

### 品牌资产

- `assets/brand/manis-mark.svg` 是软件标识，用于应用图标、界面左上角与其他小尺寸方形场景。
- `assets/brand/manis-shanshui.svg` 是高细节品牌插画，只用于文档、发布物料等大尺寸场景。
- 小于 `64px` 时只使用抽象标识，不将高细节插画缩小成图标。
- macOS 菜单栏图标继续使用单色透明模板，不直接套用带背景的彩色标识。

### 颜色语义

| Token | Light | Dark | 用途 |
|---|---|---|---|
| `surface.base` | `#F4F7F5` | `#0E1715` | 应用内容底色 |
| `surface.low` | `#EDF2EF` | `#111D1A` | 导航、解释面板 |
| `surface.high` | `#FFFFFF` | `#172521` | 编辑/列表主面 |
| `surface.chrome` | `#E7EEEA` | `#13211E` | 应用栏、状态栏 |
| `text.primary` | `#152321` | `#E3EEEA` | 主文本 |
| `text.secondary` | `#5F6E69` | `#A4B4AE` | 描述文本 |
| `text.tertiary` | `#84918D` | `#7D8E88` | 次级元数据 |
| `outline.subtle` | `#CBD6D2` | `#2B3D37` | 分隔线 |
| `outline.strong` | `#9FAFA9` | `#435851` | 输入框、插口边界 |
| `action.primary` | `#176C62` | `#79D7C6` | 主操作、选中态 |
| `action.on_primary` | `#FFFFFF` | `#082A24` | 主操作上的内容 |
| `route.trace` | `#D46642` | `#F39B75` | 当前被解释的路由轨迹 |
| `status.success` | `#24795F` | `#79D7B0` | 可用、低延迟 |
| `status.warning` | `#A96620` | `#EFB96E` | 退化、较慢 |
| `status.error` | `#B54F49` | `#EF8C84` | 失败、不可用 |
| `focus.ring` | `#0B6FBB` | `#82C8FF` | 键盘焦点 |

规则：

- `route.trace` 只表达“当前被解释的路径”，不能拿来做普通装饰。
- 成功、警告、错误都必须可被文本理解，不能只靠颜色传达。
- 深色模式不是简单反相，仍然保留浅色层次和同样的语义分层。

### 字体

- UI 使用平台系统字体。
- Windows 优先 `Segoe UI Variable`。
- macOS 优先 `SF Pro`。
- Linux 优先 `Noto Sans` 或桌面环境 UI 字体。
- 中文使用系统 CJK fallback，不做单平台字重特供。
- 数据只在域名规则、地址、速率、版本号上用等宽字体。
- 延迟、速率、序号全部使用 tabular numerals。

### 间距、圆角、动效

| Token | 值 |
|---|---|
| `space` | `4, 8, 12, 16, 24, 32` |
| `radius.control` | `8px` |
| `radius.row` | `8px` |
| `radius.pane` | `12px` |
| `radius.window` | `18px`（仅设计稿） |
| `icon.small` | `16px` |
| `icon.standard` | `18px` |
| `control.compact` | `34px` |
| `control.standard` | `38px` |
| `pointer_target.minimum` | `32px` |
| `touch_capable_target.minimum` | `44px` |

| Token | 值 |
|---|---|
| `motion.fast` | `140ms` |
| `motion.standard` | `220ms` |
| `motion.enter` | `cubic-bezier(.05, .7, .1, 1)` |
| `motion.state` | `cubic-bezier(.2, 0, 0, 1)` |
| `motion.exit` | `cubic-bezier(.3, 0, .8, .15)` / `160ms` |

动效约束：

- 唯一品牌动效是“铜色信号从规则到策略组再到节点”的传播感。
- 解释器和侧滑面板只用位移与阴影，不动画宽度。
- `prefers-reduced-motion: reduce` 下应立即切换，只保留颜色和文本状态更新。

## 布局行为

### Wide

- 内容宽度 `>= 1280`。
- 主导航 `220px`。
- 策略列表 `326px`。
- 策略详情弹性伸展。
- 路由解释器常驻 `340px`。
- 适合同时看上下文、列表、详情和解释结果。

### Medium

- 内容宽度 `900–1279`。
- 主导航压缩成 `66px` 图标轨。
- 策略列表宽 `292px`。
- 路由解释器改为右侧 side sheet，宽 `340px`。
- 打开解释器时只做平移，不重排主内容。

### Compact

- 内容宽度 `640–899`。
- 主导航压缩成 `56px` 图标轨。
- 策略列表与策略详情切换为单页面。
- 路由解释器仍然以覆盖层形式出现。
- 详情页必须提供可聚焦的返回按钮。

### 通用窗口规则

- 最小建议窗口：`640 × 560` 逻辑像素。
- 宽度变化只改变布局状态，不重建领域状态。
- 选中的策略、节点、筛选词和滚动位置都要保留。
- 平台只影响原生标题栏、菜单、快捷键标签、托盘和系统权限提示。

## 组件语言

### 应该出现的组件

- `AppChrome`：全局搜索、系统代理开关、运行态指示。
- `PrimaryNavigation`：工作区入口。
- `PolicyGroupList`：策略组摘要和选择。
- `PolicyGroupDetail`：节点、规则、设置的组合视图。
- `NodeTable`：节点选择、延迟显示、单选语义。
- `RouteInspector`：路由解释。
- `SignalPath`：三阶段路径展示。
- `RuntimeStatusBar`：聚合运行状态。

### 组件职责边界

- 列表只渲染摘要和选择动作，不直接调用 Mihomo。
- 详情页只组合节点和规则视图，不持有领域数据副本。
- 节点表提交选择意图，不在视图里做副作用。
- 解释器只展示解释结果，不自行推断规则。
- `SignalPath` 只接收已解析的 stage，不读取全局 store。

### 语言风格

- 选中态同时用表面色和单选语义表达。
- 规则行用等宽风格显示 `#18 DOMAIN-SUFFIX, youtube.com` 这类信息。
- 策略组列表强调“当前选择的出口节点”和“延迟”，不是强调配置文件。
- 解释器标题直接写“路由解释”，状态徽标明确标注“预测路径 · 演示数据”或“已观察”。

## Light / Dark 语义

Light 和 Dark 使用同一套层级关系，只改变表面、文本和强调色的相对亮度。

- `surface.base` 是整体底色。
- `surface.low` 用于导航和解释面。
- `surface.high` 用于主要工作面。
- `surface.chrome` 用于应用栏和状态栏。
- `action.primary` 用于可点击的关键动作和当前选择。
- `route.trace` 只用于解释路径，不应污染常规控件。

截图验证结果表明，浅色和深色都保持了同样的结构：左侧导航、中间工作区、右侧解释器，以及底部状态栏。

## 无障碍与键盘

### 键盘预期

| 操作 | 快捷键 |
|---|---|
| 全局搜索 | `Cmd/Ctrl + K` |
| 切换导航区域 | `Cmd/Ctrl + 1…6` |
| 聚焦策略筛选 | `/`（文本输入未聚焦时） |
| 上/下选择策略或节点 | `↑ / ↓` |
| 确认节点 | `Enter` |
| 打开/关闭路由解释 | `Cmd/Ctrl + I` / `Esc` |
| 返回 compact 列表 | `Alt + ←`，macOS 也支持 `Cmd + [` |
| 开始测速 | `Cmd/Ctrl + Shift + T` |

### 交互要求

- 焦点顺序必须跟视觉顺序一致。
- 侧滑面板打开后，首个焦点进入标题或输入框。
- 关闭后焦点回到触发按钮。
- 实时流量不要逐帧轰炸读屏器。
- 延迟数字变化不应抢焦点。
- 颜色不能单独承担状态含义，必须配合文本或图形标识。

## 路由真值语义

这是这套设计里最重要的区分。

### `本地规则预测`

- 由客户端使用与配置编译器共享的结构化规则模型做本地评估。
- 只能说明“按当前规则模型，可能会怎么走”。
- 必须显式标注为预测。
- 如果缺少进程、源地址、端口、网络类型或解析后的 IP，结果就不应被说成已确认。

### `已观察`

- 来自 Mihomo 支持的 `/connections` 控制器接口。
- 只能用于真实活跃连接。
- 可显示 `chains`、`providerChains`、`rule`、`rulePayload` 等已观测链路。
- 这是最终事实来源，不是推演。

### 产品禁令

- 不能把预测路径标成“runtime 已确认”。
- 本地评估无法确定时，应该返回“需要实际连接”，而不是猜节点。
- 解释页中的“已观察”链路只能来自连接数据。

## GPUI 交接清单

- 先把 `WindowSizeClass`、`selected_group`、`selected_node`、`filter`、`inspector` 这些状态放进稳定的 view model。
- 宽度切换只切布局，不重建领域对象。
- 异步任务都带 `generation`，只接受最新回包。
- 节点切换要保留旧值，失败后回滚并显示失败原因。
- `/connections` 相关更新要合并后再节流刷新 UI。
- 解释器、测速、规则解析都不能阻塞主线程。
- 所有列表和表格都要能虚拟化，尤其是规则和连接数据。
- 任何状态变更都要能用键盘完成，不依赖鼠标。

## 交付结论

这份设计系统文档对应的最终方向只有一个：Quiet technical surfaces, engraved labels, teal controls, and one copper route line。

宽屏保留三栏上下文，中屏把解释器收进 side sheet，窄屏改为单任务页面。路由解释永远分成“本地规则预测”和“已观察”两种真值，GPUI 的实现也应以这个边界为准。
